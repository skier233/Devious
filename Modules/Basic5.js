main();
function main()
{
    var modHistory = getModuleHistory("Basic5Hist");

    if (modHistory == 1)
    {
        Mod2X();
        return;
    }
    if(getVar("Mod2X", false))
    {
        Mod2X();
        return;
    }
    sm("%Edge%");
    startEdging();
    sm("%LetTheEdgeFade%");
    if (modHistory == 2)
    {
        Mod2A();
        return;
    }
    sm("I\'ll " + random("allow ", "give ") + "you a " + random("few seconds ", "minute ", "moment ") + "to " + random("settle down", "recover", "regain your strength"));
    sm("You\'ll need it %Grin%");
    if(getVar("Mod2B", false))
    {
        Mod2B();
        return;
    }
    sm("In the mean time I\'ll give you something to look at");
    showTaggedImage(4, ["hardcore"]);
    sm("That\'s not so nice of me is it...");
    showTaggedImage(4, ["blowjob"]);
    sm("I give you a moment to recover, then show you all these hot images %Laugh%");
    sm("Recovering is overrated anyway, %Name%");
    if(modHistory == 3)
    {
        Mod2C();
        return;
    }
    sm("%Edge%");
    startEdging();
    holdEdge();
    sm("%LetTheEdgeFade%");
    sm("I wonder how many times you can do that");
    sm("Before you go completely insane");
    if (randomInteger(1, 100) <= 50)
    {
        edgeagain();
        return;
    }
    sm("I mean, at some point you\'ll probably just edge your brain away");
    sm("Do you want to find out? %Grin%");
    sm("%Edge%");
    startEdging();
    holdEdge();
    sm("Let that %Cock% rest a little bit now");
    sm("Rub your %Balls%");
    sm("Feel how full and swollen they are");
    sm("So full of cum they could just burst");
    incrementModuleHistory("Basic5Hist");
    let answer0 = getInput("Does if feel good to rub them?");
    while (!(answer0.isLike("yes") || answer0.isLike("no") || answer0.isLike("hurt") || answer0.isLike("hurts") || answer0.isLike("hurting") || answer0.isLike("painful") || answer0.isLike("pain") || answer0.isLike("aching") || answer0.isLike("ache") || answer0.isLike("sore")))
    {
        answer0 = getInput("%YesOrNo%");
    }
    if (answer0.isLike("yes"))
    {
        sm("Good, keep rubbing them for me");
        edgeagain();
        return;
    }
    else if (answer0.isLike("no"))
    {
        sm("Maybe you\'d rather stroke your %Cock% again");
    }
    else if (answer0.isLike("hurt", "hurts", "hurting", "painful", "pain", "aching", "ache", "sore"))
    {
        sm("Aw poor you %Smile%");
    }
    sm("Squeeze your %Balls% %PetName%");
    sm("Harder! %Grin%");
    sm("You should cherish this moment, because you <i>know<> I'm going to make you edge again");
    edgeagain();
}
function edgeagain()
{
    sm("Oh those edges, they just make you so weak and horny...");
    sm("I bet you can just keep edging and edging over and over again");
    sm("%Edge%");
    startEdging();
    sm("%LetTheEdgeFade%");
    return;
    Mod2X();
}
function Mod2X()
{
    sm("%Edge%");
    startEdging();
    sm("%LetTheEdgeFade%");
    sm(random("You ought to ", "I think you should ", "You will have to ") + "do " + random("some ", "a couple ", "a few ") + "more edges, %Name%");
    sm("Because edging is " + random("what you live for ", "thing that matters most to you ", "at the centre of your world ") + "and " + random("by the way ", "incidently ", "to be honest ") + "it\'s " + random("pretty important ", "pretty darn crucial ", "key ") + "to me that you edge a lot too %Laugh%");
    sm(random("Gonna throw the dice to ", "A throw of the dice will ") + "determine how many edges you will do");
    sm("Hold on a second...", 3);
    sm("Alright found the dice %Smile%");
    incrementModuleHistory("Basic5Hist");
    sm("Here we go...");
    switch(random("a1", "a2", "a3", "a4", "a5", "a6"))
    {
        case "a1":
        a1();
        return;
        break;
        case "a2":
        a2();
        return;
        break;
        case "a3":
        a3();
        return;
        break;
        case "a4":
        a4();
        return;
        break;
        case "a5":
        a5();
        return;
        break;
        case "a6":
        a6();
        return;
        break;
    }
    a1();
}
function a1()
{
    sm("I threw a one %EmoteSad%");
    sm("Well let\'s get it over with then, edge for me %Name% %SoundEdge%");
    startEdging();
    holdEdge();
    sm("%LetTheEdgeFade%");
    sm("At least I made the most out of that one edge %Grin%");
    return;
    a2();
}
function a2()
{
    sm("It\'s a two... %EmoteSad%");
    sm("Two edges is still better than no edges, right?");
    sm("%Edge%");
    startEdging();
    holdEdge();
    sm("%LetTheEdgeFade%");
    sm("Don\'t get too comfortable %SubName%, one more to go");
    sm("%Edge%");
    startEdging();
    holdEdge();
    sm("%LetTheEdgeFade%");
    sm("Two edges can still be pretty intense");
    return;
    a3();
}
function a3()
{
    sm("It\'s a three %SubName%");
    sm("Could be better, could be worse %Smile%");
    sm("%Edge%");
    startEdging();
    sm("%LetTheEdgeFade%");
    sm("Two more to go...");
    sm("Edge again! %SoundEdge%");
    startEdging();
    sm("Relax %PetName% only one more");
    sm("%Edge%");
    startEdging();
    holdEdge();
    sm("%LetTheEdgeFade%");
    sm("I bet you %Cock% is aching pretty good right now %Smile%");
    return;
    a4();
}
function a4()
{
    sm("A four! That shouldn\'t be too much of a problem for a pro like you");
    sm("I know you savour every single edge I give you");
    sm("Lets get started %SubName%, edge for me %SoundEdge%");
    startEdging();
    sm("Almost halfway %Laugh%");
    sm("%Edge%");
    startEdging();
    sm("%LetTheEdgeFade%");
    sm("Don\'t get too comfortable now...");
    sm("%Edge%");
    startEdging();
    sm("%LetTheEdgeFade%");
    sm("And the last one... edge %PetName% %SoundEdge%");
    startEdging();
    holdEdge();
    sm("%LetTheEdgeFade%");
    sm("Breath easy now, let your body relax");
    let answer0 = getInput("Are you okay %SubName%?");
    if (answer0.isLike("yes"))
    {
        sm("Good %Smile%");
    }
    else if (answer0.isLike("no"))
    {
        sm("That was pretty intense wasn\'t it");
    }
    else if (answer0.isLike("intense", "amazing", "great"))
    {
        sm("%Grin%");
    }
    else if (answer0.isLike("mean", "cruel", "bitch"))
    {
        sm("I know %PetName% I know %Grin%");
    }
    else if (answer0.isLike("aching", "ache", "hurts", "hurting"))
    {
        sm("You\'re doing great %PetName% %Smile%");
    }
    else
    {
        sm("You\'re doing great %PetName%");
    }
    sm("Maybe next time I\'ll \"help\" the dice a little");
    sm("Too make it even harder for you %Grin%");
    return;
    a5();
}
function a5()
{
    sm("Oooh a five... make me proud %PetName% %Smile%");
    sm("Take each and every one of these edges like a true edge slut");
    sm("Give me my first edge! %SoundEdge%");
    startEdging();
    sm("%LetTheEdgeFade%");
    sm("If we do these quickly it\'ll just be like one very long edge");
    sm("%Edge%");
    startEdging();
    sm("Relax and get ready %PetName%");
    wait(5);
    sm("Edge for me again %SoundEdge%");
    startEdging();
    sm("That\'s three, only two more to go!");
    sm("%Edge%");
    startEdging();
    sm("%LetTheEdgeFade%");
    sm("You\'re doing great, get ready for the last one");
    sm("%Edge%");
    startEdging();
    holdEdge();
    sm("%LetTheEdgeFade%");
    let answer0 = getInput("Are you okay %SubName%?");
    if (answer0.isLike("yes"))
    {
        sm("Good %Smile%");
    }
    else if (answer0.isLike("no"))
    {
        sm("That was pretty intense wasn\'t it");
    }
    else if (answer0.isLike("intense", "amazing", "great"))
    {
        sm("%Grin%");
    }
    else if (answer0.isLike("mean", "cruel", "bitch"))
    {
        sm("I know %PetName% I know %Grin%");
    }
    else if (answer0.isLike("aching", "ache", "hurts", "hurting"))
    {
        sm("You\'re doing great %PetName% %Smile%");
    }
    else
    {
        sm("You\'re doing great %PetName%");
    }
    sm("May next time I\'ll use two dice %Grin%");
    return;
    a6();
}
function a6()
{
    sm("Well, guess what %SubName%");
    sm("I threw a six and I didn\'t even cheat!");
    sm("You must be so happy to get to edge a full six times for me now %Smile%");
    sm("So let\'s begin, edge for me %Name% %SoundEdge%");
    startEdging();
    sm("%LetTheEdgeFade%");
    sm("You\'ll be aching so much at the end of this %Grin%");
    sm("%Edge%");
    startEdging();
    sm("%LetTheEdgeFade%");
    wait(10);
    sm("%Edge%");
    startEdging();
    sm("This is fun, maybe I should use more dice though...");
    sm("A maximum of six edges seems a bit easy");
    sm("%Edge%");
    startEdging();
    sm("On the other hand, I don\'t want to completely ruin my toy");
    sm("Not in such a short time anyway %Grin%");
    sm("Speaking of ruining things...");
    sm("I\'m seriously considering making you ruining an orgasm after edge number 6");
    let answer0 = getInput("Damn, was that last one number 4 or 5?");
    if (answer0.isLike("4", "four", "fourth"))
    {
        sm("I think so too");
    }
    else if (answer0.isLike("5", "five", "fifth"))
    {
        sm("Mmm I don\'t think so %Name%");
    }
    else
    {
        sm("I think it was 4, that means 2 more to go");
    }
    sm("%Edge%");
    startEdging();
    holdEdge();
    sm("%LetTheEdgeFade%");
    sm("Okay get ready for the last one");
    sm("%Edge%");
    startEdging();
    sm("%LetTheEdgeFade%");
    let answer1 = getInput("So I decided not to make you ruin it after all", 8);
    if (answer1.isTimeout())
    {
        edgesenough();
        return;
    }
    if (answer1.isLike("you", "thanks"))
    {
        sm("You\'re welcome %Smile%");
    }
    else if (answer1.isLike("bad"))
    {
        sm("%Laugh% it could still happen of course...");
    }
    else if (answer1.isLike("ruin", "ruin", "it", "ruined", "ruined"))
    {
        sm("%Laugh% maybe later...");
    }
    else
    {
        sm("I was thinking more of a \"thank you\", but okay...");
    }
    edgesenough();
}
function edgesenough()
{
    sm("After all those edges I thought ruining it would have a bit much");
    sm("Like I said, I don\'t want to completely ruin my toy... yet %Grin%");
    return;
    Mod2A();
}
function Mod2A()
{
    sm("Time to do some edges %Name%");
    sm("I\'ve used dice before, but only one");
    sm("This time I want you to throw the dice yourself");
    sm("And I want you to use 2, just to " + random("make it a bit more interesting", "up the ante a bit"));
    let answer0 = getInput("So go get a pair of dice tell me when you\'re ready");
    while (!(answer0.isLike("ready") || answer0.isLike("no") || answer0.isLike("have") || answer0.isLike("got")))
    {
        answer0 = getInput("Let me know when you\'re ready");
    }
    if (answer0.isLike("ready"))
    {
        sm("Good. Now, you could cheat of course...");
        i_trust_you();
        return;
    }
    else if (answer0.isLike("no", "have", "got"))
    {
        sm("Hmm, there are other ways to do this");
    }
    sm("You can go to random.org/dice");
    sm("Or google \"dice\", I\'m sure you can find something");
    wait(20);
    let answer1 = getInput("Ready?");
    while (!(answer1.isLike("yes") || answer1.isLike("ready") || answer1.isLike("no")))
    {
        answer1 = getInput("%YesOrNo%");
    }
    if (answer1.isLike("yes", "ready"))
    {
        sm("Good. Now, you could cheat of course...");
        i_trust_you();
        return;
    }
    else if (answer1.isLike("no"))
    {
        sm("God... just make sure you have a pair of dice next time, %PetName%");
    }
    sm("We\'ll just have to move on to something else I guess");
    return;
    i_trust_you();
}
function i_trust_you()
{
    sm("But I trust you to do exactly as instructed %Smile%");
    sm("Okay %Name%, throw the dice!");
    let answer0 = getInput("What number did you get?");
    while (!(answer0.isLike("1") || answer0.isLike("one") || answer0.isLike("2") || answer0.isLike("two") || answer0.isLike("3") || answer0.isLike("three") || answer0.isLike("4") || answer0.isLike("four") || answer0.isLike("5") || answer0.isLike("five") || answer0.isLike("6") || answer0.isLike("six") || answer0.isLike("7") || answer0.isLike("seven") || answer0.isLike("8") || answer0.isLike("eight") || answer0.isLike("9") || answer0.isLike("nine") || answer0.isLike("10") || answer0.isLike("ten") || answer0.isLike("11") || answer0.isLike("eleven") || answer0.isLike("12") || answer0.isLike("twelve")))
    {
        answer0 = getInput("Seriously %SubName%, what number?");
    }
    while (answer0.isLike("1", "one"))
    {
        answer0 = getInput("Very funny, what number did you really get?");
    }
    if (answer0.isLike("2", "two"))
    {
        sm("Damn, lucky you...");
        setVar("Mod2Aedges", 2);
    }
    else if (answer0.isLike("3", "three"))
    {
        sm("Only 3... it\'ll have to do");
        setVar("Mod2Aedges", 3);
    }
    else if (answer0.isLike("4", "four"))
    {
        sm("Could be higher");
        setVar("Mod2Aedges", 4);
    }
    else if (answer0.isLike("5", "five"))
    {
        sm("Okay then");
        setVar("Mod2Aedges", 5);
    }
    else if (answer0.isLike("6", "six"))
    {
        sm("I\'d be happier had it been one die");
        setVar("Mod2Aedges", 6);
    }
    else if (answer0.isLike("7", "seven"))
    {
        sm("Could be worse");
        setVar("Mod2Aedges", 7);
    }
    else if (answer0.isLike("8", "eight"))
    {
        sm("Not bad");
        setVar("Mod2Aedges", 8);
    }
    else if (answer0.isLike("9", "nine"))
    {
        sm("That\'s not bad");
        setVar("Mod2Aedges", 9);
    }
    else if (answer0.isLike("10", "ten"))
    {
        sm("Alright, double digits!");
        setVar("Mod2Aedges", 10);
    }
    else if (answer0.isLike("11", "eleven"))
    {
        sm("Alright, double digits!");
        setVar("Mod2Aedges", 11);
    }
    else if (answer0.isLike("12", "twelve"))
    {
        sm("That\'s what I like to see!");
        setVar("Mod2Aedges", 12);
    }
    sm("Let\'s get it over with %Smile%");
    back_to_the_top();
}
function back_to_the_top()
{
    sm("%Edge%");
    startEdging();
    sm("%LetTheEdgeFade%");
    setVar("Mod2Aedges", getVar("Mod2Aedges", 0) - 1);
    if (getVar("Mod2Aedges", 0) == 0)
    {
        all_edges_done();
        return;
    }
    sm("%Mod02EdgesLeft%");
    back_to_the_top();
    return;
    all_edges_done();
}
function all_edges_done()
{
    sm("That\'s all for now %Name%");
    sm("Although to be honest, that was probably not your last edge %GeneralTime%");
    incrementModuleHistory("Basic5Hist");
    return;
    two_need_break();
}

function Mod2B()
{
    sm("Do you know what time it is?");
    sm("Time to bring out the dice and do some edges %Smile%");
    sm("You don't mind doing some edges, I'm sure... you <i>never<> mind doing some edges");
    sm("Even when you think you don\'t want any more edges, you still want more edges");
    sm("It\'s the truth and you know it, %PetName%");
    let answer0 = getInput("Now, I think last time you used 2 dice, right?", 8);
    if (answer0.isTimeout())
    {
        positive_it_was();
        return;
    }
    while (!(answer0.isLike("yes") || answer0.isLike("no")))
    {
        answer0 = getInput("%YesOrNo%");
    }
    if (answer0.isLike("yes"))
    {
        sm("I think you can probably guess what I\'m going to say next");
        positive_it_was();
        return;
    }
    else if (answer0.isLike("no"))
    {
        sm("I\'m pretty sure we used 2 dice last time, %PetName%");
    }
    sm("And since the time before that we used 1...");
    positive_it_was();
}
function positive_it_was()
{
    sm("This time we\'ll use 3 %Grin%");
    incrementModuleHistory("Basic5Hist");
    let answer0 = getInput("Are you up for it?");
    while (!(answer0.isLike("yes") || answer0.isLike("no")))
    {
        answer0 = getInput("%YesOrNo%");
    }
    if (answer0.isLike("yes"))
    {
        sm("I expected nothing else from you, %SubName% %Smile%");
    }
    else if (answer0.isLike("no"))
    {
        sm("That was a rhetorical question, it\'s not up for debate");
    }
    sm("So go get 3 dice, or go to random.org/dice or some other website");
    sm("And let\'s see how many edges you get");
    let answer1 = getInput("Ready?");
    while (!(answer1.isLike("yes") || answer1.isLike("ready") || answer1.isLike("no") || answer1.isLike("don't") || answer1.isLike("won't") || answer1.isLike("can't") || answer1.isLike("please") || answer1.isLike("not") || answer1.isLike("possible") || answer1.isLike("impossible") || answer1.isLike("going")))
    {
        answer1 = getInput("Are you ready?");
    }
    if (answer1.isLike("yes", "ready"))
    {
        sm("%Grin%");
        throw_three_dice();
        return;
    }
    else if (answer1.isLike("no"))
    {
        sm("I'll wait", 4);
    }
    else if (answer1.isLike("don't", "won't", "can't", "please", "not", "possible", "impossible", "going"))
    {
        sm("So, this is going to be a problem...");
    }
    let answer2 = getInput("Are you going to get the dice, yes or no?");
    while (!(answer2.isLike("yes") || answer2.isLike("no")))
    {
        answer2 = getInput("%YesOrNo%");
    }
    if (answer2.isLike("yes"))
    {
        sm("Okay, good %Smile%");
        throw_three_dice();
        return;
    }
    else if (answer2.isLike("no"))
    {
        sm("I\'m disappointed %SubName%");
    }
    //TODO: add CBT here --Command:BallTorture
    sm("And I\'m going to take it out on your %Balls%");
    //run("Custom" + java.io.File.separator + "Instructions" + java.io.File.separator + "CBTBalls.js");
    sm("Make sure next time you have 3 dice");
    increaseAnger(3)
    return;
    throw_three_dice();
}
function throw_three_dice()
{
    sm("Throw the dice!");
    let answer0 = getInput("What number did you get?");
    while (!(answer0.isLike("1") || answer0.isLike("one") || answer0.isLike("2") || answer0.isLike("two") || answer0.isLike("3") || answer0.isLike("three") || answer0.isLike("4") || answer0.isLike("four") || answer0.isLike("5") || answer0.isLike("five") || answer0.isLike("6") || answer0.isLike("six") || answer0.isLike("7") || answer0.isLike("seven") || answer0.isLike("8") || answer0.isLike("eight") || answer0.isLike("9") || answer0.isLike("nine") || answer0.isLike("10") || answer0.isLike("ten") || answer0.isLike("11") || answer0.isLike("eleven") || answer0.isLike("12") || answer0.isLike("twelve") || answer0.isLike("13") || answer0.isLike("thirteen") || answer0.isLike("14") || answer0.isLike("fourteen") || answer0.isLike("15") || answer0.isLike("fifteen") || answer0.isLike("16") || answer0.isLike("sixteen") || answer0.isLike("17") || answer0.isLike("seventeen") || answer0.isLike("18") || answer0.isLike("eighteen")))
    {
        answer0 = getInput("Seriously %SubName%, what number?");
    }
    while (answer0.isLike("1", "one"))
    {
        answer0 = getInput("Very funny, what number did you really get?");
    }
    while (answer0.isLike("2", "two"))
    {
        answer0 = getInput("Right, with 3 dice... so what number did you really get?");
    }
    if (answer0.isLike("3", "three"))
    {
        sm("What are the odds... %EmoteSad%");
        setVar("Mod2Aedges", 3);
    }
    else if (answer0.isLike("4", "four"))
    {
        sm("Meh...");
        setVar("Mod2Aedges", 4);
    }
    else if (answer0.isLike("5", "five"))
    {
        sm("Meh...");
        setVar("Mod2Aedges", 5);
    }
    else if (answer0.isLike("6", "six"))
    {
        sm("Six it is");
        setVar("Mod2Aedges", 6);
    }
    else if (answer0.isLike("7", "seven"))
    {
        sm("Lucky seven! Lucky for you, that is...");
        setVar("Mod2Aedges", 7);
    }
    else if (answer0.isLike("8", "eight"))
    {
        sm("Only 8... lucky you");
        setVar("Mod2Aedges", 8);
    }
    else if (answer0.isLike("9", "nine"))
    {
        sm("That\'s not bad");
        setVar("Mod2Aedges", 9);
    }
    else if (answer0.isLike("10", "ten"))
    {
        sm("10 is a nice round number, a bit low though");
        setVar("Mod2Aedges", 10);
    }
    else if (answer0.isLike("11", "eleven"))
    {
        sm("I\'m slightly disappointed %EmoteWink%");
        setVar("Mod2Aedges", 11);
    }
    else if (answer0.isLike("12", "twelve"))
    {
        sm("That\'ll do %PetName%, that\'ll do %Smile%");
        setVar("Mod2Aedges", 12);
    }
    else if (answer0.isLike("13", "thirteen"))
    {
        sm("Lucky 13 %Smile%");
        setVar("Mod2Aedges", 13);
    }
    else if (answer0.isLike("14", "fourteen"))
    {
        sm("That\'s not too bad");
        setVar("Mod2Aedges", 14);
    }
    else if (answer0.isLike("15", "fifteen"))
    {
        sm("That\'s not bad, I\'m happy %Smile%");
        setVar("Mod2Aedges", 15);
    }
    else if (answer0.isLike("16", "sixteen"))
    {
        sm("Sweet sixteen %Smile%");
        setVar("Mod2Aedges", 16);
    }
    else if (answer0.isLike("17", "seventeen"))
    {
        sm("Oh yeah %Laugh%");
        setVar("Mod2Aedges", 17);
    }
    else if (answer0.isLike("18", "eighteen"))
    {
        sm("This is going to take a while %Laugh%");
        setVar("Mod2Aedges", 18);
    }
    sm("Let\'s " + random("do this ", "get started ", "get to it ", "begin ", "have it then ") + "%Smile%");
    three_back_to_top();
}
function three_back_to_top()
{
    sm("%Edge%");
    startEdging();
    sm("%LetTheEdgeFade%");
    setVar("Mod2Aedges", getVar("Mod2Aedges", 0) - 1);
    if (getVar("Mod2Aedges", 0) < 1)
    {
        three_edges_done();
        return;
    }
    sm("%Mod02EdgesLeft%");
    three_back_to_top();
    return;
    three_edges_done();
}
function three_edges_done()
{
    sm("That\'s all of them %Smile%");
    sm("That poor %Cock% of yours must be throbbing now %Grin%");
    return;
    three_need_break();
}

function Mod2C()
{
    sm("Let\'s do some more edging");
    sm("Because let\'s face it, you want nothing more than to be on the edge");
    sm("And I think you become a better sub for me with every second you are on the edge");
    sm("So don\'t even try and pretend you\'d rather be doing anything other than to edge your fucking brains out for me right know");
    resetModuleHistory("Basic5Hist");
    let answer0 = getInput("Do you have 3 dice ready?");
    while (!(answer0.isLike("yes") || answer0.isLike("no")))
    {
        answer0 = getInput("%YesOrNo%");
    }
    if (answer0.isLike("yes"))
    {
        sm("Let\'s see how lucky you are today...");
        throw_three_dice();
        return;
    }
    else if (answer0.isLike("no"))
    {
    }
    sm("Oh...");
    let answer1 = getInput("But you\'re going to get them for me right away, right?");
    while (!(answer1.isLike("ready") || answer1.isLike("yes") || answer1.isLike("course") || answer1.isLike("certainly") || answer1.isLike("wish") || answer1.isLike("no")))
    {
        answer1 = getInput("%YesOrNo%");
    }
    while(answer1.isLike("yes", "course", "certainly", "wish"))
    {
        sm("Good, let me know when you\'re ready");
    }
    if (answer1.isLike("ready"))
    {
        sm("Let\'s see how lucky you are today...");
        throw_three_dice();
        return;
    }
    else if (answer1.isLike("no"))
    {
        sm("What, you don\'t like edging anymore all of a sudden?");
        increaseAnger(3)
    }
    //TODO: add cbt here--Command:CockTorture
    sm("Maybe you\'d prefer hurting yourself...");
    //run("Custom" + java.io.File.separator + "Instructions" + java.io.File.separator + "CBTCock.js");
    sm("Make sure you have 3 dice next time");
    sm("Remember, you can also use websites like " + "org/dice");
    return;
}